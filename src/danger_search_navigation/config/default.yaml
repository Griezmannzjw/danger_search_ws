# nav_controller.py 的节点私有参数。competition.launch 已在 navigation 节点内加载本文件。

# 坐标系和冻结接口
map_frame: map
base_frame: base
lidar_frame: laser_livox
nav_cmd_topic: /danger_search/nav_cmd_vel
pose_topic: /localization/pose
map_topic: /map
mapping_status_topic: /mapping/status
health_topic: /navigation/health
obstacle_cloud_topic: /scan
safety_stop_topic: /danger_search/safety_stop
move_base_action_name: /move_base
make_plan_service: /move_base/make_plan
clear_costmaps_service: /move_base/clear_costmaps

# 共享 OccupancyGrid A*。未知、占据和所有非自由格均不可通行。
occupied_threshold: 65
robot_radius: 0.30
inflation_padding: 0.10
allow_diagonal: true
max_planner_expansions: 75000
# 严格已知区规划失败后，Action 可高代价进入 unknown；真实/动态障碍仍阻断。
action_unknown_penalty: 3.0

# 消息合法性和新鲜度门。pose/map 的源时间由 /mapping/status 统一判定；
# 这两个值保留用于参数兼容，导航不再用它们淘汰合法的缓存地图/位姿。
pose_timeout: 2.00
map_timeout: 5.00
mapping_status_timeout: 1.50
obstacle_cloud_timeout: 1.50
goal_stamp_timeout: 2.00
max_future_stamp_skew: 0.05
quaternion_norm_tolerance: 0.05
# 若为 true，缺失、过期或帧错误的 /scan 会安全结束活动目标。
require_obstacle_cloud: true

# /scan 到 base 的部署外参与局部动态障碍筛选。点云仅做障碍保护，不用于定位。
lidar_x: 0.20
lidar_y: 0.00
lidar_z: 0.08
lidar_roll: 0.00
lidar_pitch: 0.785
lidar_yaw: 0.00
# Ground returns cluster near -0.24 m in base. Keep only obstacles that rise
# into the robot body envelope so ground samples cannot seal the local plan.
obstacle_min_z: -0.10
obstacle_max_z: 0.80
obstacle_range_min: 0.15
obstacle_range_max: 8.00
zero_point_radius: 0.05
obstacle_self_exclusion_min_x: -0.55
obstacle_self_exclusion_max_x: 0.55
obstacle_self_exclusion_half_width_y: 0.40
obstacle_max_points: 2000
dynamic_stop_distance: 0.60
dynamic_front_half_angle: 0.52

# 非完整底盘的路径跟踪、最终朝向和有限频率重规划。
control_rate: 20.0
health_rate: 5.0
goal_timeout: 60.0
goal_tolerance_xy: 0.30
goal_tolerance_yaw: 0.20
lookahead_distance: 0.60
cruise_speed: 0.20
max_linear_speed: 0.30
max_angular_speed: 0.80
rotate_in_place_angle: 0.45
rotate_in_place_gain: 1.50
final_yaw_gain: 1.20
replan_period: 1.00
replan_min_interval: 0.25
replan_deviation_distance: 0.50
stuck_timeout: 5.00
# 活动 Action 的数据短时退化先零速等待，超时后才结束同一目标。
readiness_recovery_timeout: 3.00
progress_distance: 0.10
stuck_command_speed: 0.05
